def solution(x):
    ll = list(str(x))
    add = 0
    
    for i in range(len(ll)):
        add += int(ll[i]) 
        if x % add == 0: 
            answer = True 
        else: 
            answer = False
    return answer
