# 조짐ㅋ 일단 조금씩 더 수정 해보고있음 ㅋㅋ
# 화이팅!

def solution(new_id):
    lower = new_id.lower()
    if type(lower) is str:
        pass
    if '-' in lower:
        pass
    if '.' in lower:
        pass
    if '_' in lower:
        pass
    else : lower.strip("")
        
    for i in range(lower):
        if lower[i] == lower[i+1]:
            replace = lower.replace('..','.')
    answer = ''
    
    if lower.startswith('.').replace('.','')
    if lower.endswith('.').replace('.','')
    if lower is None:
        lower.replace('','a')
    if len(lower) < 2:
        add = lower[-1]
        while len(lower) == 3:
            lower+add
        
    answer = lower
    
    return answer
